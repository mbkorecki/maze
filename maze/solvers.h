#ifndef MAIN_H
#define MAIN_H
#include "stack.h"
#include "queue.h"

int dfsSolver (int *mazeWalls[], int maxX, int maxY, int draw);
int bfsSolver(int *mazeWalls[], int maxX, int maxY, int draw);

#endif
